#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../../zx@4.3.0/node_modules/zx/zx.mjs" "$@"
else
  exec node  "$basedir/../../../../../../zx@4.3.0/node_modules/zx/zx.mjs" "$@"
fi
